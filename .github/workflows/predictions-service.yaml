name: Build and test PredictionsService

on:
  pull_request:
    branches:
      - master
      - release/predictionsservice*/**
    paths:
      - 'dev/predictionsservice/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./src/Saver.PredictionsService

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Install dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release

    - name: Check for vulnerabilities
      run: |
        dotnet list package --vulnerable --include-transitive 2>&1 | tee build.log
        echo "Analyze dotnet list package command log output..."
        grep -q -i "critical\|high\|moderate\|low" build.log; [ $? -eq 0 ] && echo "Security Vulnerabilities found on the log output" && exit 1

    - name: Test
      working-directory: ./test/Saver.PredictionsService.Tests
      run: dotnet test --no-build --verbosity normal
